/* eslint-disable no-empty-function */
import ResizeObserver from '.';

// eslint-disable-next-line no-unused-vars
const obs = new ResizeObserver((entries: ResizeObserverEntry[], observer: ResizeObserver) => {});
const elm: any = {};

obs.observe(elm);
